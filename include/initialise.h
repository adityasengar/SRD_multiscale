#ifndef INITIALISE_H_
#define INITIALISE_H_

#include <stdio.h>
#include "variables.h" // Include variables.h for global variable declarations

// No need for extern declarations here, as they are in variables.h
// This file will primarily contain function prototypes for initialization if any,
// but based on the original code, initial_conditions() is in main.c.

// If there were specific initialization functions for external libraries or complex structures,
// their prototypes would go here. For now, it's mostly a placeholder for future expansion.

#endif /* INITIALISE_H_ */
